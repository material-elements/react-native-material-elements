import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  appBarContainer: {
    width: '100%',
    paddingVertical: 6,
    paddingHorizontal: 8,
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
  appBarItemContainer: {
    padding: 4,
  },
});
